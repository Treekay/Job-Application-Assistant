{
  "name": "ai-job-application-assistant",
  "version": "1.0.0",
  "private": true,
  "description": "Job application workflow platform with .NET API, React client, reminders, and AI-assisted match summaries.",
  "scripts": {
    "dev": "concurrently \"dotnet run --project api/JobWorkflow.Api\" \"npm run dev --workspace client\"",
    "dev:api": "dotnet run --project api/JobWorkflow.Api",
    "dev:client": "npm run dev --workspace client",
    "build:api": "dotnet build JobWorkflow.sln",
    "build": "npm run build --workspace client",
    "start": "dotnet run --project api/JobWorkflow.Api"
  },
  "workspaces": [
    "client"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
